import { WeatherAlertTypes } from './weather-alert-types.model';

describe('WeatherAlertTypes', () => {
  it('should create an instance', () => {
    expect(new WeatherAlertTypes()).toBeTruthy();
  });
});
